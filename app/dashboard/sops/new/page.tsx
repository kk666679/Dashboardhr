export default function NewSopPage() {
  // Placeholder: SOP wizard wiring exists in components, but current environment type-checking
  // cannot resolve Next import/types consistently. Keep page minimal.
  return (
    <div className="w-full p-4">
      <h1 className="text-xl font-semibold">Create SOP</h1>
      <p className="text-sm opacity-70">Wizard UI placeholder.</p>
    </div>
  );
}

